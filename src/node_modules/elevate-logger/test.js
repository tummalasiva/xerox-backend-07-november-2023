const { elevateLog }  = require("./index");


// var logger = new log("First name", "Last name");
async function ab(){
    let data = elevateLog.config("info","elevate4",false);
    let logger = elevateLog.init();

    let req = {
        triggerNotification:true,
        request:"123",
        message:"hide"
    }
    logger.info(req);
    // logger.info("abc3");

}

ab();


// elevateLog.loggerConfig();

